public enum IngredientsNames { }

public enum MeasurmentType
{
    grams,
    liters,
    spoons,
}

public enum BaseProductNames
{
    SweetDough,
    SourDough,
}

public enum Products { }

public enum Allergens
{
    Milk,
    Nuts,
    Sesame,
    Egg,
    Sulfites,
    Gluten,
}
